[Unit]
Description=Dev Services Nginx Manager
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'nginx -t && systemctl reload nginx'
User=root
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target